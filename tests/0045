# Tests that the first pen input is accepted correctly

monitor notice off
monitor xb3a off

# Boot and run until we reach the screen that prompts the user for a tap
wait 20000

# After tapping the screen, wait for the update to finally happen
input pen 10,10
wait 1000

print CRC
exit
